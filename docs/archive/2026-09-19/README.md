# Debugging evidence

`admin-css-response.html` is the former root `_admin-css.tmp`: a saved HTML 404 response from an earlier asset diagnostic. It is retained for reference and is not a stylesheet or application input.
