# Service pages override

Master applies.

- Landings, not SaaS pricing grids.
- Quote form hidden until Request a Quote; then DNA inputs in two columns from 768px.
- Primary CTA matches Article buttons.
- Process is a numbered sequence (real sequence — numbers allowed).
- FAQ is buttons + regions, not cards.
